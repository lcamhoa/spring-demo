app:
  datasource:
    employee:
      driver-class-name: org.h2.Driver
      jdbcUrl: jdbc:h2:mem:employee;DB_CLOSE_DELAY=-1
      username: root
      password: rootpw
    payroll:
      driver-class-name: org.h2.Driver
      jdbcUrl: jdbc:h2:mem:payroll;DB_CLOSE_DELAY=-1
      username: root
      password: rootpw
